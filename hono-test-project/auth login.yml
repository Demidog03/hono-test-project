info:
  name: auth login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/auth/login
  body:
    type: json
    data: |-
      {
        "name": "Olzhas Otep",
        "email": "olzhas1@gmail.com",
        "password": "Qwerty123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
